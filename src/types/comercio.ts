/**
 * Comercio — entrada de la sección "Esto es para ti".
 */
export interface Comercio {
  id: string;
  name: string;
  /** URL/import del logo. */
  logo: string;
  /** Clase Tailwind de fondo del círculo (p.ej. "bg-km0-teal-50"). */
  bg: string;
}
